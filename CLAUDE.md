# kyoto-2026 — 響け！ユーフォニアム 聖地巡礼ウェブアプリ

京都・宇治の聖地巡礼旅行（2026.08.28–30・2名）のための静的サイト。
`sutadonman/okinawa-2026` をテンプレートとして派生させたもの。GitHub Pages で公開する。

**公開URL（予定）**: https://sutadonman.github.io/kyoto-2026/

---

## 何を作るのか

| ファイル | 役割 | 状態 |
|---|---|---|
| `spots.js` | **全59スポットのデータ。唯一の情報源** | ✅ 用意済み |
| `index.html` | 旅程表＋巡礼チェックリスト＋進捗 | ✅ 完成（チェック同期込み） |
| `mochimono.html` | 持ち物チェックリスト（ローカル保存のみ） | ✅ 完成 |
| `seisan.html` | 立替・精算（沖縄版からほぼ流用） | ✅ 完成（デザインは index に統一） |
| `apps-script.gs` | 受け口。チェック同期シートを追加 | ✅ 完成・デプロイ反映済み |
| `kyoto-spots.csv` | マイマップ取り込み用。`spots.js` から生成 | ✅ 完成（`build-csv.mjs` で生成） |

**座標収集ページ（`coords.html`）は作らない。** 沖縄版と違って観光ポイントの提出は不要で、
スポットの追加・修正は `spots.js` を直接更新する運用にしたため。地図は `q`（検索語）による
埋め込みで足りている。サーバー側の `coord_add` と `座標収集` シートも作らない。

## 沖縄版との違い（設計判断の記録）

沖縄版はカード25枚を `index.html` に直書きしていた。京都版は**データを `spots.js` に外出しして描画で生成する**。理由は3つ。

1. **同じ情報の重複** — 沖縄版はカード・座標一覧テーブル・CSVの3箇所に同じスポットが載っている。京都版はさらにチェック同期ID・営業時間・シーンメタデータが増えるため、直書きだと1スポットが4〜5箇所に散る
2. **行程の変更頻度** — 企画段階で既に2回大きく組み替わっている（苔寺・鈴虫寺の出入り、中華の日移動、銀閣寺の追加）。配列なら要素の移動で済む
3. **カードの均質性** — 「久美子ベンチ」「自販機前」のように短く形の揃ったカードが大半で、テンプレート化が効く

その他の相違点。

| 沖縄版 | 京都版 |
|---|---|
| レンタカー移動 | 徒歩＋電車/バス（`travelmode=walking` / `transit`） |
| 25スポット | 59スポット（うち移動6件） |
| 3名（Otsu/Sugi/Runto） | **2名（Yoshi/Oka）** |
| 座標は施設代表点でOK | ファン聖地8件は**実測が必要**（後述） |
| 営業時間は緩め | **営業時間が行程の要**（鳳飛12:00–13:30 など） |
| 希望リストの投票・採否 | 不要。スポットは `spots.js` を直接更新する |

## 実装するもの

### 1. index.html — 旅程表＋チェックリスト

沖縄版の `index.html` のCSS・カードの見た目・`.chip` / `.pin` / `.card-foot` の構造はそのまま踏襲する。変えるのは中身の生成方法とチェック機能。

- `spots.js` を読み、Day別セクション → カードを生成
- 各カードにチェックボックス。Day見出しに進捗（例 `Day1 18/27`）。宿と移動は分母から外す
- `kind` でチップ表示（聖地/観光/飯/宿）、`works` があれば話数チップ
- `.pin` リンクは `coords` があれば `?api=1&query=lat,lng`、無ければ `query=<q>`
- 埋め込み地図は**全スポット**に置く。IntersectionObserver で画面に入ったものだけ iframe を作るので、件数が増えても初期表示は重くならない
- ルートボタンは `maps/dir` を使うが、**ウェイポイントは9件程度が上限**。11地点ずつに自動で区切り、区間を1件重ねて連結する（Day1は33スポットで3区間）
- `<meta name="robots" content="noindex">` を入れる（沖縄版と同様）

### 2. チェック同期（localStorage + Apps Script）

**必ずローカル優先で書く。** 宇治は徒歩移動で圏外になる場所がある。

```
チェックを押す
  → localStorage に即書き＋UIを即反映
  → 裏で Apps Script に POST
  → 失敗したら未送信キューに積む
  → online イベント / 次回起動時にキューを flush
```

シートは1行1チェックで `spotId / member / checkedAt`。
「Yoshiは行ったがOkaは未」を表現できるので、別行動にも耐える。
既存の希望リスト（1枚目）・立替（2枚目）には触らず、**3枚目のシートとして追加**する。

### 3. 営業時間の警告

`spots.js` に `checkOpen()` と `tightWindow()` を用意済み。

- `checkOpen(spot)` — 訪問曜日が `closed` に当たっていれば警告。Day1=金/Day2=土/Day3=日
- `tightWindow(spot, 120)` — 営業枠が2時間以下のものを検出。鳳飛（12:00–13:30）が引っかかる
- `spot.warn` に手書きの注意文が入っているものはそのまま強調表示

現時点では全スポット定休日に当たっていない（確認済み）。行程が動いたときに崩れるのを検出するのが主目的。

### 4. 座標が未確定の8件

施設として存在せず、Places で引けないもの。`spots.js` 内の note に `★座標未確定` と記載してある。

**専用の収集ページは作らない。** 現地で確認したら `spots.js` の `coords` を直接書く。
差分が git に残るので、そのほうが追いやすい。`index.html` の末尾にこの8件の一覧が出る。

座標が入るまでは `q` の検索語で埋め込み地図とピンのリンクを出す。実際の地点とは
数十m〜のズレが出るが、最初の行程表と同じ挙動なので実用上は足りている。

| id | スポット | 備考 |
|---|---|---|
| `d1-060` | 通学路 | 道の途中。代表点を1つ決める |
| `d1-100` | 志を繋ぐ碑 | 公園内の碑の位置 |
| `d1-120` | 宇治橋東詰交差点 | 交差点の一点 |
| `d1-160` | 水門前 | 観流橋の水門前 |
| `d1-170` | 久美子ベンチ | 朝霧橋付近のベンチ |
| `d1-180` | 自販機前 | 喜撰橋手前の自販機 |
| `d1-230` | 宇治川水管橋 | 橋の**真下の河川敷**（橋自体ではない） |
| `d1-240` | 大吉山 展望台 | 山頂の展望台 |

残り51件は `q` の検索語で地図が引ける見込み。

### 5. seisan.html — 立替・精算

沖縄版からほぼそのまま流用。変更点だけ。

- `apps-script.gs` の `MEMBERS` を `['Yoshi', 'Oka']` に
- `seisan.html` のラジオボタンも同じ2名に
- **2名なので送金は常に1回**。最小送金回数の探索ロジックは残しておいてよい（2名なら自明な答えを返すだけ）
- 端数ルール（割り切れない分は立替者に寄せる）はそのまま
- ダッシュボードの色分けは3名分用意されているので変更不要

### 6. kyoto-spots.csv

`spots.js` から生成する。沖縄版と同じ列構成にすると My Maps の手順が変わらない。

```
名前,日程,種別,緯度,経度,メモ
```

- `名前` は `01 京都駅` のように**訪問順の番号を前置**する（マイマップで順番が見えるように）
- UTF-8 **BOM付き**で出力（Excelでの文字化け回避。沖縄版と同じ）
- 座標未確定のものは、暫定的に `q` の検索語を入れた列を使う運用でもよい

## Apps Script まわりの引き継ぎ

### 決定：沖縄版のスプレッドシートとデプロイを共用する

**`SHEET_ID` / `TOKEN` / ウェブアプリURL（`/exec`）はすべて沖縄版のまま流用する。**
新規デプロイは作らない。再認証も不要（権限は変わらない）。
`index.html` / `seisan.html` の `API_URL` には沖縄版と同じURLを入れる。

シート構成（同一スプレッドシート内）:

| # | シート名 | 用途 | 旅行 |
|---|---|---|---|
| 1 | （既存・1枚目） | スポット希望リスト | 沖縄 |
| 2 | `立替・精算` | 立替・精算 | 沖縄 |
| 3 | `巡礼チェック` | spotId / member / checkedAt | **京都** |
| 4 | `立替・精算_京都` | 立替・精算 | **京都** |

3枚目は `checkSheet_()` が、4枚目は `expSheet_(trip)` が、初回アクセス時に
末尾へ自動で作る。既存の1〜2枚目は動かない。

### ⚠️ 共用で最も注意すべき点：`MEMBERS` の衝突

スプレッドシートを共用するとApps Scriptも共用になる（コンテナバインドのため）。
沖縄版の `MEMBERS = ['Otsu','Sugi','Runto']` を `['Yoshi','Oka']` に**書き換えてはいけない**。
沖縄旅行は2026.10.08–11で**京都より後、まだ本番稼働中**。書き換えると沖縄側の
希望リストと精算が全部 `invalid` で弾かれる。

旅行ごとにスコープを切る。既存の定数はそのまま残し、追加する形にする。

```js
// 既存はそのまま（沖縄のデフォルト。trip 未指定のリクエストはこちらに落ちる）
var MEMBERS = ['Otsu', 'Sugi', 'Runto'];

var TRIPS = {
  okinawa: { members: MEMBERS,            expenseSheet: EXPENSE_SHEET },
  kyoto:   { members: ['Yoshi', 'Oka'],   expenseSheet: '立替・精算_京都' },
};
function tripOf_(body) { return TRIPS[body && body.trip] || TRIPS.okinawa; }
```

リクエストに `trip: 'kyoto'` を載せ、`MEMBERS.indexOf(who)` の検証を
`tripOf_(body).members.indexOf(who)` に置き換える。
`expFields_` / `addExpense_` / `updateExpense_` / `removeExpense_` / `normTargets_` が
`MEMBERS` を直接参照しているので、そこを trip 経由に通す。

### ⚠️ 後方互換を壊さないこと

沖縄版のページ（`wishlist.html` / `seisan.html`）は**すでに公開・稼働中**で、
リクエストに `trip` を持たない。よって:

- `trip` 省略時は必ず沖縄として振る舞うこと（上の `tripOf_` はそうなっている）
- 既存アクション（`addItems_` / `updateItem_` / `setStatus_` / `setKind_` / `removeItem_`）の
  引数と応答形式を変えないこと
- スクリプトを更新したら**デプロイを管理 → 鉛筆 → 新バージョン**で反映する。
  新規デプロイにするとURLが変わり、沖縄版のページが動かなくなる
- 更新後は沖縄側のフォームからも1件テスト送信して、壊れていないことを確認する

### 既存コードの構造（`apps-script.gs`）

- `sheet_()` — 1枚目（希望リスト・沖縄）
- `expSheet_()` — `立替・精算`。無ければ自動生成し見出しも書く
  → **trip に応じてシート名を切り替える形に拡張する**
- → **`checkSheet_()` を同じ流儀で追加する**（3枚目・巡礼チェック）
- `doGet` / `doPost` — `TOKEN` 照合してからアクションで分岐
- `withLock_()` — 同時書き込みのロック。チェック同期でも必ず通す
- `resolveShort_()` / `expandUrl_()` / `parsePlace_()` — 共有URL展開。沖縄の希望リスト専用。京都では使わない

`ensureExpHeaders_` / `ensureExpIds_` は京都用シートにもそのまま効く（シート参照を差し替えるだけ）。

**再デプロイの注意**（沖縄版で踏んだ罠）:
権限が増えたときは再デプロイだけでは承認画面が出ず、**黙って失敗する**。
`appsscript.json` を更新 → `authorize` 関数を手動実行して承認 → デプロイを管理 → 新バージョン、の順で行う。
今回は権限が増えないので通常は不要。

## Places API

**京都版では使っていない。** 地図は `output=embed` の埋め込みとピンのリンクだけで足りており、
キーを必要とする呼び出しが無い。沖縄版が Places を叩いていたのは写真の取得のためで、
京都版は写真を置かない（下記「やらないこと」）ので出番がない。

将来使う場合は沖縄版のキーを使いまわせる。同一ドメイン（`sutadonman.github.io`）なので、
HTTPリファラー制限がかかっていればサブパスが変わっても通る。その際は呼び出し数を抑えるため、
**`coords` が埋まっているスポットは Places を叩かない**分岐にすること。
全件を毎回引くとキーの消費が倍増するうえ、ファン聖地に誤ったピンが立つ。

## やらないこと

- **アニメ本編のスクリーンショットを置かない。** 京アニの著作物であり、GitHub Pages は公開サイトになる。
  代わりに「自分で撮った写真をチェックイン時に紐づける」枠を用意する方向で検討する
- 聖地巡礼ブログ等から写真を転載しない（撮影者の著作物。クレジットを付けても転載可にはならない）

## 作る順番

1. ✅ `index.html` — `spots.js` を読んでカード生成＋チェック（localStorageのみ）
2. ✅ `kyoto-spots.csv` の生成 — マイマップに取り込んで座標の当たり具合を確認
3. ✅ `apps-script.gs` のチェック同期 — localStorage の未送信キューから送る
   - スプレッドシートは沖縄版と共用。`TRIPS` で trip スコープを切ってから着手する
   - 更新後は沖縄側のフォームからもテスト送信して回帰がないか確認する
4. ✅ `seisan.html` — trip=kyoto を載せて移植（`MEMBERS` は書き換えず追加）
5. PWA化（Service Worker / manifest）— 余力があれば。オフラインで旅程表が開ける

1が動けば旅行には使える。4以降は出発までに間に合えばよい。

## 参照

- 元リポジトリ: https://github.com/sutadonman/okinawa-2026
- 元リポジトリの README に、Apps Script のデプロイ手順・短縮URLの罠・マイマップ取り込み手順が詳しく書かれている。**そちらを先に読むこと**
